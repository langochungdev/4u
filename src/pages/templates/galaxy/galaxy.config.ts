import type { TemplateConfig } from '@/config/templates';

const config: TemplateConfig = {
    maxImages: 4,
    maxVideos: 0,
    maxAudios: 0,
    maxContent: 4,
    templateName: 'galaxy'
};

export default config;
