import type { TemplateConfig } from '@/config/templates';

const config: TemplateConfig = {
    maxImages: 1,
    maxVideos: 0,
    maxAudios: 0,
    maxContent: 2,
    templateName: 'demo'
};

export default config;
