import type { TemplateConfig } from '@/config/templates';

const config: TemplateConfig = {
    maxImages: 1,
    maxVideos: 1,
    maxAudios: 1,
    maxContent: 2,
    templateName: 'demo'
};

export default config;
