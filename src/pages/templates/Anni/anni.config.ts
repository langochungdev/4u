import type { TemplateConfig } from '@/config/templates';

const config: TemplateConfig = {
  templateName: 'demoTrac',
  maxImages: 5,
  maxVideos: 0,
  maxAudios: 0,
  maxContent: 2
};

export default config;
