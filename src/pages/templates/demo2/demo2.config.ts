import type { TemplateConfig } from '@/config/templates';

const config: TemplateConfig = {
    maxImages: 2,
    maxVideos: 2,
    maxAudios: 2,
    maxContent: 2,
    templateName: 'demo2'
};

export default config;
