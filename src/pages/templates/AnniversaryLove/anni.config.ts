import type { TemplateConfig } from '@/config/templates';

const config: TemplateConfig = {
  templateName: 'AnniversaryLove',
  maxImages: 5,
  maxVideos: 0,
  maxAudios: 0,
  maxContent: 3
};

export default config;
